{
  "name": "qat-explainer",
  "private": true,
  "version": "1.0.0",
  "description": "Interactive quantization-aware training explainer. The page has no dependencies beyond D3 from cdnjs; the dev dependency drives it in headless Chrome for the tests.",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\"",
    "serve": "python3 -m http.server 8000 --bind 0.0.0.0"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "packageManager": "pnpm@10.34.5"
}
